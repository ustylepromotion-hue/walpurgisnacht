# ワルプルBOT 引き継ぎ

更新日: 2026-09-09 / 最新変更は本番反映済み。新規の作業指示が来るまで追加実装しない。

## 再開場所
- ユーザー指定フォルダ: `/Users/ust/Desktop/ワルプルBOT`
- 実装・ビルド用チェックアウト: `/Users/ust/dev/walpurgis-ui`（node_modulesあり）
- ソースは指定フォルダにも同期済み。次回は差分を確認してから編集する。
- 本番: https://dax-place.com/walpurgisnacht/ （末尾スラッシュなしは308で転送）
- Cloudflare Worker: `walpurgisnacht`
- 最終デプロイVersion: `bb96132f-7502-43f1-ab1d-e3d3375fdb37`
- 初期試作品のSites URLと `.openai/hosting.json` は履歴。現在の本番はユーザー指定のCloudflare Worker。Sitesへ再デプロイしない。
- Gitでは初期UI以降の変更が未コミット。ファイルには保存済み。初期コミットへ戻さない。

## ユーザーの方針
- 回答・進捗は簡潔に。指示前の作業は禁止。
- ライトヴァイオレットのChatGPT風UI。スマホで使いやすく、下部に控えめなスポンサー予定枠。
- BOTは寄り添う柔らかい秘書のような口調。役柄を名乗らず、過剰な敬語・媚び・絵文字・話題提案・末尾の会話勧誘を避ける。
- 共感より設定の正確さを優先。願いの文言と動機、対象と作用、能力と結果を混同しない。誤りには穏やかに補足。
- 願いの比較例: マルグリートは魔法少女を救いたい、まどかは魔女を生まれる前に消し去りたい。共通の動機を根拠に同じ願いとしない。
- ワルプルギスの構成例: マルグリート個人だけでなく、救いきれなかった名もなき少女たちの呪いが重なった集合体。
- 上記の補足と元資料は運営者提供情報。独立に公式確認済みとは扱わない。
- デプロイがガードで拒否された場合、回避せず理由とユーザー実行コマンドを渡す。直近のデプロイはガードなしで成功。

## 実装
- React / vinext / Vite / Cloudflare Workers。Node.js 22.13以上。
- `app/page.tsx`: チャット、テーマ別質問カード、セッション内会話切替、コピー、送信待ち、モバイルメニュー。
- `app/globals.css`: 紫テーマ、レスポンシブ、考察中スピナー、出力カーソル。
- `lib/chat.ts`: 自サイトAPI呼び出し。直近19メッセージ・合計18000文字以内。
- `lib/typewriter.ts`: 全文受信後に一文字（書記素）ずつ描画。約16ms以上の間隔、フレームごとに最大一文字。長い中断後も一括追い出ししない。Intl.Segmenter非対応端末にフォールバック。
- スマホ送信時はソフトキーボードを閉じる。文字送り中は会話欄内だけを追従スクロールし、上に読みに戻った場合は追従停止。
- 文字送りは動きを減らす設定でも行う。スピナーやカーソル点滅はその設定で抑制。
- `server/worker.ts`: パス制御、末尾スラッシュ転送、`/walpurgisnacht/api/chat`、UI配信。
- `server/chat-api.ts`: DeepSeek呼び出し、入力サイズ・role検証、同一origin確認、エラーの秘匿、最大90秒、最大出力2048トークン。
- DeepSeek公式 `https://api.deepseek.com/chat/completions`、モデル `deepseek-v4-flash`（公式では0731版に更新済みと確認）。thinking disabled、temperature 0.2。
- シークレット名 `walpurgisnacht_KEY`。値はCloudflare登録済み。取得・表示・クライアント埋め込みは不要。
- `wrangler.jsonc`: 本番Worker設定。LLM_PROVIDER=deepseek。CHAT_RATE_LIMITER=IP単位12回/60秒。拠点単位の緩い制限で、厳密な費用上限ではない。
- `server/prompt.ts`: 基本方針とRESPONSE_VOICE（柔らかな口調）。
- `server/knowledge.ts`: 提供された元の考察資料をサーバー専用文字列として収録。
- `server/editorial-context.ts`: 原資料の不足を補う構造化訂正。出典・願い・対象・作用・時点・能力・結果・混同禁止を分けて記録。
- `server/context.ts`: 基本方針→原資料→編集補足→区別ルール→口調、の順で構成。
- 元の `walpurgis-complete-bot-source-v2.md` は指定フォルダに保持。自動上書きしない。

## 確認結果と限界
- 最新ビルド、TypeScript、自作ファイルlint、APIテスト成功。
- 文字送りは60fps/30fps、一文字単位、旧端末用文字分割、バックグラウンド復帰、中断をテスト済み。
- 本番ページ・JS/CSS配信と実LLM回答を確認済み。
- 解釈回帰5ケース（直接比較、同一視の誘導、過去の誤回答の訂正、対象混同、動機と願い）で区別を確認。
- 最新口調変更後にも、感情への応答と願いの比較を本番確認。共感表現を出し、願いの違いを保持。
- スマホ実機・ブラウザ操作による表示確認は未実施。文字送りテストは描画スケジューラのシミュレーション。
- LLMの全回答の正確さや語調を保証するものではない。実例が出たら資料の不足・層の混同・指示競合を調べ、文言追加だけで済ませない。
- 全体lintには雛形同梱UI部品の既存指摘あり。自作部分は確認済み。

## コマンド
ビルド用チェックアウトで実行:
```sh
cd /Users/ust/dev/walpurgis-ui
npm test
node tests/typewriter.test.mjs
npx tsc --noEmit
npm run build
npm run deploy
```
- `npm run eval:live`: 公開APIへ5件の質問を送り、課金対象の実推論を実行。必要な変更時だけ実行。単語チェック後に回答の意味を人が読む。
- ローカル開発: `npm run dev`。本番用ビルドのローカル配信: `npm start`。
- ローカルには本番キーを保存していないので、キーなしAPIは503になるのが正常。
- 起動したローカル確認サーバーは前の作業で停止済み。他プロジェクトのサーバーを止めない。

## 未実装・次の候補（新たな指示が必要）
- 投稿された考察の自動蓄積、D1、管理画面、承認・マージ機能。
- ユーザーは以前この構想を相談。Cloudflare自動蓄積→管理者確認→採用分だけ反映を提案したが、まだ実装指示はない。
- 現状会話はメモリのみ、再読み込みで消える。AIに保存・学習済みと称させない。
- スポンサー枠は準備中表示。募集フォーム、広告配信、連絡先は未設定。
